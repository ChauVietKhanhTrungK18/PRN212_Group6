<Window x:Class="Task_Management_System.CreateTaskWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Task_Management_System"
        mc:Ignorable="d"
        Title="Create Task" Height="650" Width="600" WindowStartupLocation="CenterScreen" ResizeMode="CanResize">
    <Grid Background="#f4f6fa" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Create New Task" FontSize="24" FontWeight="Bold" Margin="0,0,0,10"/>
            <TextBlock x:Name="txtProjectInfo" Text="Project: " FontSize="14" FontWeight="Bold"/>
        </StackPanel>

        <!-- Task Information -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <GroupBox Header="Task Information" Margin="0,0,0,15">
                    <Grid Margin="10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Task Name:" FontWeight="Bold" Margin="0,5"/>
                        <TextBox x:Name="txtTaskName" Grid.Row="0" Grid.Column="1" Margin="10,5"/>

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="Description:" FontWeight="Bold" Margin="0,5"/>
                        <TextBox x:Name="txtDescription" Grid.Row="1" Grid.Column="1" Margin="10,5" 
                                 Height="80" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"/>

                        <TextBlock Grid.Row="2" Grid.Column="0" Text="Deadline:" FontWeight="Bold" Margin="0,5"/>
                        <DatePicker x:Name="dpDeadline" Grid.Row="2" Grid.Column="1" Margin="10,5" 
                                    SelectedDateChanged="dpDeadline_SelectedDateChanged"/>

                        <TextBlock Grid.Row="3" Grid.Column="0" Text="Status (Auto):" FontWeight="Bold" Margin="0,5"/>
                        <TextBlock x:Name="txtStatusPreview" Grid.Row="3" Grid.Column="1" Margin="10,5" 
                                   Text="Will be determined automatically" FontSize="12" Foreground="#666" VerticalAlignment="Center"/>
                    </Grid>
                </GroupBox>

                <GroupBox Header="Assign Members" Margin="0,0,0,15">
                    <Grid Margin="10">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                            <TextBlock Text="Available Members:" VerticalAlignment="Center" Margin="0,0,10,0" FontWeight="Bold"/>
                            <Button Content="Select All" Width="80" Height="25" Margin="5,0" Click="BtnSelectAll_Click"/>
                            <Button Content="Clear All" Width="80" Height="25" Margin="5,0" Click="BtnClearAll_Click"/>
                        </StackPanel>

                        <ListBox x:Name="lbMembers" Grid.Row="1" Height="150" SelectionMode="Multiple">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <CheckBox IsChecked="{Binding IsSelected}" Margin="0,0,10,0"/>
                                        <TextBlock Text="{Binding FullName}" VerticalAlignment="Center"/>
                                        <TextBlock Text=" (" Foreground="Gray" VerticalAlignment="Center"/>
                                        <TextBlock Text="{Binding Email}" Foreground="Gray" VerticalAlignment="Center"/>
                                        <TextBlock Text=")" Foreground="Gray" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </Grid>
                </GroupBox>
            </StackPanel>
        </ScrollViewer>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
            <Button Content="Create Task" Width="120" Height="35" Margin="5" 
                    Background="#28a745" Foreground="White" FontWeight="Bold" Click="BtnCreateTask_Click"/>
            <Button Content="Cancel" Width="120" Height="35" Margin="5" 
                    Background="#6c757d" Foreground="White" FontWeight="Bold" Click="BtnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window> 